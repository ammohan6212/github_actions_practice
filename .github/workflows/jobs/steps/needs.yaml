jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::set-output name=result::Build Success"
        id: build-step

  test:
    runs-on: ubuntu-latest
    needs: build  # This job depends on 'build' finishing
    steps:
      - run: echo "Test triggered after build"
