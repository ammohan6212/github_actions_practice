name: deploy the application to the dev env 
run-name: dploye is triggered by ${{github.actor}}


on:
  push: 
    branches:
      - dev
jobs:
  deploy: 
    name: displaying job 
    runs-on: ubuntu-latest 

    steps:
      - name: checkout code 
        run: echo "this is the first job"