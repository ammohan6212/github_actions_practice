name: deploy the application to the dev env 



on:
  push: 
    branches:
      - dev
run-name: dploye is triggered by ${{github.actor}}  on branh ${{github.ref_name}} on ${{github.repository}}
##thiss one display as the heading the pipeline section



jobs:
  deploy: 
    name: displaying job 
    runs-on: ubuntu-latest 

    steps:
      - name: checkout code 
        run: echo "this is the first job"